extends layout
include utils.pug

block content
    link(rel="stylesheet" href="/stylesheets/homepage.css")
    div(id="banner")
    div(id="game-display")
        h1 What's New?
        if games
            div(class='img-box')
                each game in games
                    +gameCard(game)
